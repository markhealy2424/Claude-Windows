// TODO: integrate pdf-parse / pdfjs + OCR (Tesseract or cloud).
// Returns array of { pageNumber, text, kind } where kind ∈ floor|elevation|window_schedule|door_schedule|unknown.
export async function extractPlan(_pdfBuffer) {
  return [];
}

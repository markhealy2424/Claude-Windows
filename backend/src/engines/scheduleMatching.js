// TODO: parse window/door schedule pages, join with detected marks.
// Returns array conforming to the data model in REQUIREMENTS.md §2.4.
export function matchSchedule(_marks, _pages) {
  return [];
}
